{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for teurtask desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "deep-link:default"
  ]
}
